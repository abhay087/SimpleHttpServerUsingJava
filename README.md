This is a simple HTTP server implemented in Java. This server responds to every request it receives
with the current date and time, and a message saying “you have got response
from the server”. It’s a very basic example of an HTTP server, but it demonstrates the fundamental concepts of how a server works
